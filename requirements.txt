# Runtime dependencies for the deployed API (kept slim for Vercel).
# For local model TRAINING / RL, also install requirements-dev.txt.
fastapi>=0.111
uvicorn[standard]>=0.30
pandas>=2.2
numpy>=1.26
requests>=2.32
python-dotenv>=1.0
tiktoken>=0.7          # optional; token_optimizer falls back if absent
